pipeline {
    agent any

    environment {

        DOCKER_DEV_REGISTRY="https://harbor.longguikeji.com"
    }
    stages {
        stage('pre') {
            steps {
                sh "echo $DOCKER_DEV_REGISTRY;source ./pipeline.groovy; echo $DOCKER_DEV_REGISTRY"
            }
        }
    }
}
