pipeline {
    agent any

    environment {

        DOCKER_DEV_REGISTRY="https://harbor.longguikeji.com"
        load 'pipeline.groovy'
    }
    stages {
        stage('pre') {
            steps {
                sh "echo $DOCKER_DEV_REGISTRY"
            }
        }
    }
}
